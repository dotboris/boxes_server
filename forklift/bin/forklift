#!/usr/bin/env ruby
$:.unshift File.expand_path('../../lib', __FILE__)

require 'boxes'
require 'forklift'

puts 'Forklift reporting in!'

conn = Boxes.bunny
conn.start

ch = conn.create_channel
q = ch.queue 'boxes.slices.load'

q.subscribe block: true do
  puts 'Got load request'
end